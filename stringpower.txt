#include <bits/stdc++.h>

using namespace std;

int main()
{
       string str;
       cin>>str;
      string pattern1="heavy";
       string pattern2="metal";
        vector<int>v1,v2;
    int found1 = str.find(pattern1);

  // int j=0;
    while (found1 != string::npos) {
        
       v1.push_back(found1);
        
        found1 = str.find(pattern1, found1 + 1);
    
      int found1 = str.find(pattern1);
    }

     int found2 = str.find(pattern2);
     
    while (found2 != string::npos) {
        
       v2.push_back(found2);
        
        found2 = str.find(pattern2, found2 + 1);
    
     int found2 = str.find(pattern2);
    }
     for(int i=0;i<v1.size();i++)
     {
 //         cout<<v1[i]<<" ";
     }
  //  cout<<endl;
     for(int i=0;i<v2.size();i++)
     {
 //         cout<<v2[i]<<" ";
     }
//    cout<<endl;
   std::vector<int>::iterator upper1;
  long long int c=0,s=0;
  s=v2.size();
  for(int i=0;i<v1.size();i++)
  {
  upper1 = std::upper_bound(v2.begin(), v2.end(),v1[i]); 
  c=c+(s-(upper1-v2.begin()));
  }
    cout<<c<<endl;
    
    return 0;
}