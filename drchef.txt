#include <bits/stdc++.h>

using namespace std;
  
int main() 
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    long long int t;
    cin>>t;
    while(t--)
    {
     long long int sum=0;
        long long int n,x;
        cin>>n>>x;
        long long int arr[n];
        for(int i=0;i<n;i++)
        {
             cin>>arr[i];
        }
        long long int value=0;
       long long int ind=0,c=0,cp=0;
       long long int day=0;
        sort(arr,arr+n);
     
        while(ind!=n)
        {
            ind = upper_bound(arr+c, arr+n,x) - arr; 
             if(ind==n)
              break;
            if(ind>0)
            {
                 value=(arr[ind-1]*2);
                 if(value>=x)
                 {
                      arr[ind-1]=0;
                      c=ind;
                      day++;
                      x=value;
                 }
                 else
                 {
                      x=x*2;
                      day++;
                 }
            }
            else
            {
                 x=x*2;
                 day++;
            }
        }
        long long int ct=0;
      // cout<<arr[0]<<endl;
        for(int i=0;i<n;i++)
        {
             if(arr[i]>0)
             ct++;
        }
        
       //cout<<day<<endl;
      //cout<<ct<<endl;
    
     sum=ct+day;
     cout<<sum<<endl;
       
    }

  return 0;
}